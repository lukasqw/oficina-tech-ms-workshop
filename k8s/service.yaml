apiVersion: v1
kind: Service
metadata:
  name: ${APP_NAME}-service
  namespace: ${NAMESPACE}
spec:
  type: NodePort
  selector:
    app: ${APP_NAME}
  ports:
    - port: ${SERVER_PORT}
      targetPort: ${SERVER_PORT}
      nodePort: 30083
      protocol: TCP
      name: http
